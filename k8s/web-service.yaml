apiVersion: v1
kind: Service
metadata:
  name: ecommerce-web-service
spec:
  type: NodePort
  selector:
    app: ecommerce-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
